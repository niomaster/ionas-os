#include "io.h"

void writeXY(int x, int y, char *text)
{
    char *screenBuffer = (char *)0xb800;
    char *pos = screenBuffer + (x*2) + (y*2*80);
    for(int i = 0; i < strlen(text); i++)
    {
        screenBuffer[pos] = text[i];
        pos += 2;
    }
}
